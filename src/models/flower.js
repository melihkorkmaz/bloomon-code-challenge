/**
 * Flower.
 * @class
 */
class Flower {
  /**
   * Default constructor.
   * @param {string} name
   * @param {string} size
   */
  constructor(name, size) {
    this.name = name
    this.size = size
  }
}

module.exports = Flower
